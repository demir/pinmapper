<div class="dropleft" data-controller="dropdown">
  <%= link_to '#', class: 'add_to_board_btn', id: 'add-pin-to-board-menu', data: { toggle: 'dropdown', 'aria-haspopup': true, 'aria-expanded': false } do %><% end %>

  <div class="dropdown-menu add-pin-to-board" aria-labelledby="add-pin-to-board-menu" data-action="click->dropdown#stopPropagation">
    <div class="px-3 py-3">
      <div class="header">
        <span><%= t('boards.add_to_board') %></span>
      </div>
      <div class="body">
        <%= render(Boards::ListItemForPinComponent.with_collection(current_user.boards, pin: pin, current_user: current_user)) %>
      </div>
    </div>
  </div>
</div>
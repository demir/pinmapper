<%= form_with url: path, class: 'search-form', method: :get, data: { controller: 'search', 'search-url-value': path, action: 'keydown->search#disable_enter' } do |form| %>
  <%= form.text_field 'name', class: 'form-control', placeholder: t('search.search'), autocomplete: 'off', data: { action: 'input->search#search' } %>
<% end %>

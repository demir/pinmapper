<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <% @pins.each do |slug, updated_at| %>
    <url>
      <loc><%= pin_url(slug, locale: nil) %></loc>
      <lastmod><%= updated_at.strftime("%F") %></lastmod>
    </url>
  <% end %>
</urlset>